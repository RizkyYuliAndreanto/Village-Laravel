# 🛡️ PRODUCTION ENVIRONMENT TEMPLATE
# Copy file ini ke .env dan edit dengan values production yang sebenarnya

# ===================================================================
# PRODUCTION CONFIGURATION - VILLAGE WEB
# Target Deployment: 26 Januari 2026
# ===================================================================

# Application Configuration
APP_NAME="Desa Banyukambang"
APP_ENV=production
APP_KEY=base64:GENERATE_WITH_php_artisan_key:generate
APP_DEBUG=false
APP_URL=https://your-production-domain.com
APP_TIMEZONE=Asia/Jakarta

# Locale Configuration
APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID

# ===================================================================
# SECURITY CONFIGURATION (CRITICAL)
# ===================================================================

# HTTPS Security (WAJIB untuk production)
FORCE_HTTPS=true
FORCE_HTTPS_LOCAL=false

# Admin Security - GOVERNMENT FRIENDLY CONFIGURATION
# Mode 'warning' = Log saja, tidak block akses (RECOMMENDED untuk Pemdes)
# Mode 'strict' = Block akses dari IP tidak terdaftar (untuk environment dengan IT support)
ADMIN_IP_MODE=warning
ADMIN_IP_AUTO_LEARN=true
ADMIN_IP_GRACE_PERIOD=24

# IP Allowlist - Opsional untuk mode warning (sistem akan auto-learn)
ADMIN_IP_ALLOWLIST=127.0.0.1
# Contoh lengkap:
# ADMIN_IP_ALLOWLIST=127.0.0.1,192.168.1.0/24,OFFICE_IP,KADES_HOME_IP

# Security Notifications - BUDGET FRIENDLY DEFAULT
SECURITY_EMAIL_ALERTS=false
SECURITY_ADMIN_EMAIL=
SECURITY_SLACK_WEBHOOK=

# Rate Limiting
SECURITY_RATE_LIMIT=60
SECURITY_RATE_WINDOW=1

# Auto-ban Configuration
SECURITY_AUTO_BAN_ENABLED=true

# ===================================================================
# DATABASE CONFIGURATION (UPDATE dengan real credentials)
# ===================================================================

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=your_production_database_name
DB_USERNAME=your_secure_username
DB_PASSWORD=your_very_strong_password_16_chars_min

# ===================================================================
# SHARED HOSTING OPTIMIZED SETTINGS
# ===================================================================

# Cache Configuration (File-based untuk shared hosting)
CACHE_STORE=file
CACHE_PREFIX=village_cache

# Session Configuration (File-based untuk shared hosting)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

# Queue Configuration (Sync untuk shared hosting)
QUEUE_CONNECTION=sync

# File Storage (Public untuk shared hosting)
FILESYSTEM_DISK=public

# ===================================================================
# PRODUCTION OPTIMIZATION
# ===================================================================

# PHP Configuration
PHP_CLI_SERVER_WORKERS=1
BCRYPT_ROUNDS=12

# Logging Configuration
LOG_CHANNEL=single
LOG_LEVEL=warning
LOG_DEPRECATIONS_CHANNEL=null

# Maintenance Mode
APP_MAINTENANCE_DRIVER=file

# ===================================================================
# MAIL CONFIGURATION (UPDATE dengan SMTP real)
# ===================================================================

MAIL_MAILER=smtp
MAIL_HOST=smtp.your-hosting-provider.com
MAIL_PORT=587
MAIL_USERNAME=noreply@your-domain.com
MAIL_PASSWORD=your_email_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@your-domain.com
MAIL_FROM_NAME="${APP_NAME}"

# ===================================================================
# OPTIONAL: THIRD PARTY SERVICES
# ===================================================================

# MinIO/S3 (Optional - jika menggunakan external storage)
MINIO_ENDPOINT=
MINIO_KEY=
MINIO_SECRET=
MINIO_REGION=
MINIO_BUCKET=

# AWS (Optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

# ===================================================================
# FILAMENT ADMIN CONFIGURATION
# ===================================================================

FILAMENT_ADMIN_PATH=admin
FILAMENT_ADMIN_MIDDLEWARE=web,auth

# ===================================================================
# PRODUCTION SECURITY NOTES
# ===================================================================

# IMPORTANT SECURITY CHECKLIST:
# 1. ✅ Generate fresh APP_KEY: php artisan key:generate
# 2. ✅ Update ADMIN_IP_ALLOWLIST dengan IP real Anda
# 3. ✅ Setup HTTPS certificate di hosting
# 4. ✅ Update database credentials dengan real values
# 5. ✅ Configure SMTP email settings
# 6. ✅ Set file permissions: chmod 755 storage/ bootstrap/cache/
# 7. ✅ Test semua functionality sebelum go-live
# 8. ✅ Setup backup strategy
# 9. ✅ Configure monitoring & alerts
# 10. ✅ Run: php artisan optimize:shared-hosting --setup --cache-warmup

# ===================================================================