# ===================================================================
# SHARED HOSTING ENVIRONMENT CONFIGURATION
# Optimized for shared hosting providers (Hostinger, Niagahoster, etc.)
# ===================================================================

# Application Configuration
APP_NAME="Desa Banyukambang"
APP_ENV=production
APP_KEY=base64:GENERATE_NEW_KEY_WITH_php_artisan_key:generate
APP_DEBUG=false
APP_URL=https://your-domain.com
APP_TIMEZONE=Asia/Jakarta

# Locale Configuration
APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID

# Shared Hosting Maintenance (use file instead of cache)
APP_MAINTENANCE_DRIVER=file

# PHP Configuration (shared hosting limits)
PHP_CLI_SERVER_WORKERS=1
BCRYPT_ROUNDS=10

# Security Configuration - GOVERNMENT FRIENDLY & BUDGET FRIENDLY
SECURITY_EMAIL_ALERTS=false
SECURITY_ADMIN_EMAIL=
SECURITY_SLACK_WEBHOOK=

# ===================================================================
# EMAIL CONFIGURATION - BUDGET FRIENDLY (DISABLED)
# Monitoring via dashboard saja: /security-admin/dashboard
# ===================================================================

# Email disabled untuk hemat biaya setup
MAIL_MAILER=log
# MAIL_HOST=  # Tidak perlu SMTP setup
# MAIL_PORT=  # Hemat konfigurasi 
# MAIL_USERNAME=  # Tidak perlu email account
# MAIL_PASSWORD=  # Tidak perlu password
# MAIL_ENCRYPTION=
# MAIL_FROM_ADDRESS=
# MAIL_FROM_NAME=

# ===================================================================
# ALTERNATIVE: JIKA SUATU HARI MAU AKTIFKAN EMAIL
# Uncomment dan sesuaikan jika budget tersedia untuk setup email
# ===================================================================

# GMAIL OPTION (Jika suatu hari mau aktifkan):
# SECURITY_EMAIL_ALERTS=true
# SECURITY_ADMIN_EMAIL=admin@desa-domain.com
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-gmail@gmail.com
# MAIL_PASSWORD=your_app_password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=your-gmail@gmail.com
# MAIL_FROM_NAME="Website Desa Security"

# Admin IP Configuration - MODE PEMERINTAHAN DESA
# Mode 'warning' = Tidak block, hanya log (RECOMMENDED untuk desa)
# Mode 'strict' = Block akses dari IP tidak dikenal
ADMIN_IP_MODE=warning
ADMIN_IP_AUTO_LEARN=true  
ADMIN_IP_GRACE_PERIOD=24
ADMIN_IP_ALLOWLIST=127.0.0.1

# Database Configuration (shared hosting MySQL)
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password

# Session Configuration (FILE DRIVER for shared hosting)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=.your-domain.com
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax

# Cache Configuration (FILE DRIVER for shared hosting)
CACHE_STORE=file
CACHE_PREFIX=village_cache

# Queue Configuration (SYNC for shared hosting - no background processing)
QUEUE_CONNECTION=sync

# Broadcasting Configuration (LOG for shared hosting)
BROADCAST_CONNECTION=log

# Filesystem Configuration (LOCAL STORAGE)
FILESYSTEM_DISK=public

# Mail Configuration (shared hosting SMTP)
MAIL_MAILER=smtp
MAIL_HOST=your-shared-hosting-smtp
MAIL_PORT=587
MAIL_USERNAME=your-email@domain.com
MAIL_PASSWORD=your_email_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@your-domain.com"
MAIL_FROM_NAME="${APP_NAME}"

# Shared Hosting Optimizations
SHARED_HOSTING_MODE=true
CACHE_VIEWS=true
CACHE_CONFIG=true
CACHE_ROUTES=true

# Security Configuration (adapted for shared hosting)
SECURITY_ENABLE_RATE_LIMITING=true
SECURITY_ENABLE_DDOS_PROTECTION=true
SECURITY_ENABLE_AUTO_BAN=false

# Performance Settings (conservative for shared hosting)
MAX_EXECUTION_TIME=30
MEMORY_LIMIT=128M
MAX_INPUT_VARS=1000

# File Upload Limits (shared hosting restrictions)
MAX_FILE_SIZE=2048
MAX_FILES_PER_REQUEST=5

# Disable features not available on shared hosting
REDIS_CLIENT=
REDIS_HOST=
REDIS_PASSWORD=
REDIS_PORT=

# Database Connection Pooling (disabled for shared hosting)
DB_POOL_MIN=1
DB_POOL_MAX=5

# Asset URL (if using subdomain for static files)
# ASSET_URL=https://static.your-domain.com

# Vite Configuration
VITE_APP_NAME="${APP_NAME}"
VITE_APP_URL="${APP_URL}"